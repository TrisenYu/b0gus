Camouflaged http and https in 80/443

HTTP(s) bogus server is much more complicated to implement

which services?
Nginx? Apache?
or Render page by PHP etc?
which domains do we maintain?
    phone, nickname, email, password?
    HTTP methods/path
    cookie



package misc_utils

// Once executing b0gus, if we still want to interact with b0gus,
// then a possible way of implementation is to let b0gus listen at a assigned port,
// which can be access from configuration as well.

// We can control the interaction by coding some special rules.
